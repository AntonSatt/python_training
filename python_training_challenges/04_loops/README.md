# Challenge 4: Loops

## The Problem
The `sum_numbers.py` has broken loop logic for summing a list of numbers.

## Your Task
Fix the `sum_list` function so it correctly:
1. Iterates through all numbers in the list
2. Returns the total sum

## Expected Output
```
Sum: 15
```

## Run the tests
```bash
python test_sum_numbers.py
```
